[package]
name = "db_migration"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
sea-orm-migration = { version = "1", features = [
    "runtime-tokio-rustls",
    "sqlx-postgres",
] }
tokio = { version = "1.0", features = ["full"] }
